{
  "name": "FastAPI Dev Container",
  "build": {
    "dockerfile": "../Dockerfile",
    "context": ".."
  },
  "postCreateCommand": "/bin/bash -lc 'pip install -r requirements.txt && curl -fsSL https://raw.githubusercontent.com/ohmybash/oh-my-bash/master/tools/install.sh | bash'"
}
